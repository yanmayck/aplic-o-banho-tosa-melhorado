# Environment Variables

# Application
PORT=3000

# JWT
# Generate a strong secret key (e.g., using openssl rand -hex 32)
JWT_SECRET=YOUR_STRONG_JWT_SECRET_KEY_HERE
JWT_EXPIRES_IN=3600s

# Database (Prisma)
# Example for PostgreSQL. Adjust for your database type.
# DATABASE_URL="postgresql://user:password@host:port/database?schema=public"
DATABASE_URL="postgresql://postgres:docker@localhost:5432/furryfriends?schema=public"

# Bcrypt Salt Rounds (Optional, defaults to 10 if not set)
# BCRYPT_SALT_ROUNDS=10

